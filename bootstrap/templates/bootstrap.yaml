apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: bootstrap
  namespace: argocd
spec:
  descriptions: bootstrap app of apps pattern
  sourceRepos:
    - {{ .Values.destinationServer }}
  destination:
    - namespace: {{ .Values.namespace }}
      server: {{ .Values.repoUrl }}
